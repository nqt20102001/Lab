@model IEnumerable<Lab.Application.DTOs.CatalogDTO>

<div class="container mt-5">
    <h2 class="mb-4">Catalog</h2>

    <a class="btn btn-primary mb-3" asp-action="Create">Create New Catalog</a>

    <table class="table table-hover table-bordered align-middle">
        <thead class="table-dark">
            <tr>
                <th>Title</th>
                <th>Description</th>
                <th>Is Active</th>
                <th scope="col" class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var catalog in Model)
            {
                <tr>
                    <td>@catalog.Title</td>
                    <td>@catalog.Description</td>
                    <td>@catalog.IsActive</td>
                    <td class="text-center">
                        <a class="btn btn-sm btn-info text-white" asp-action="Edit" asp-route-id="@catalog.Id">Edit</a> |
                        <a class="btn btn-sm btn-secondary" asp-action="Details" asp-route-id="@catalog.Id">Details</a> |
                        <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@catalog.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
